# ╔══════════════════════════════════════════════════════════╗
# ║  ОФЛАЙН-УСТАНОВКА (выполняется ОДНОРАЗОВО при наличии ║
# ║  интернета), далее проект работает полностью без сети) ║
# ╚══════════════════════════════════════════════════════════╝
#
#pip download -r requirements.txt -d ./packages
#pip install --no-index --find-links=./packages -r requirements.txt
#huggingface-cli download sentence-transformers/all-MiniLM-L6-v2 --local-dir models_cache

# Core ML / NLP
sentence-transformers==5.6.1
transformers==5.14.1
torch==2.13.0
safetensors==0.8.0
huggingface_hub==1.24.0
tokenizers==0.22.2

# Vector search
qdrant-client==1.18.0
faiss-cpu==1.14.3

# ML utilities
numpy==2.5.1
scikit-learn==1.9.0
scipy==1.18.0

# Visualization
matplotlib==3.11.1

# HTTP / networking
httpx==0.28.1
qdrant-client==1.18.0

# Utilities
tqdm==4.69.1
filelock==3.32.0
fsspec==2026.6.0
packaging==26.2
